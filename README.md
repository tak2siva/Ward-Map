# Ward-Map

To start the app,
```sh
$ npm install
$ react-native start --reset-cache
$ react-native run-android
```
To check logs,
```sh
$ react-native log-android
```
For android logs
```sh
$ adb logcat
```
